function myFunction(){//declares the function
    var sentence="Hiya ^^, "; sentence+="How are you?";//assigning a variable called sentence and creating a concatenated sentence using +=
    document.getElementById("button").innerHTML=sentence;//returns the element to the id attribute button and displays our sentence
}
